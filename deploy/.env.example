# deploy/docker-compose.yml 용 환경 변수. 실제 값은 서버의 deploy/.env 에 (커밋 금지).

# fish-tank 전용 PostgreSQL 비밀번호 (내부 네트워크 전용이지만 강한 값 권장)
FISHTANK_DB_PASSWORD=

# Azure AD(Entra) 앱 등록의 애플리케이션(클라이언트) ID / 테넌트 ID
TEAMS_APP_CLIENT_ID=
TEAMS_TENANT_ID=
